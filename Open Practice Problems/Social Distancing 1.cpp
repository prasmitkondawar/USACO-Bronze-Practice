#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <bits/stdc++.h>
using namespace std;

int main() {
    long n;
    cin >> n;
    string s = "";
    cin >> s;

    for(int i = 0; i < n; i++) {
        if(s[i] == "1" && i != 0) {
            while
        }
    }
}