#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <bits/stdc++.h>
using namespace std;

int distance(int x1, int y1, int x2, int y2) {
    return (int) (sqrt(((x2 - x1) * (x2 - x1)) + ((y2 - y1) * (y2 - y1))));
}

int main() {
    int n, p;
    // vector<int> CCWdistance
}